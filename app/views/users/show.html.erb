<div class = "booyah-box">
  <div class = "col-xs-4">
    <%= image_tag "user.png" %><br />
    <button type="button" class="btn btn-primary" data-dismiss="modal">Upload a Profile Pic</button>
  </div>

  <div class = "col-xs-8">
    <h1><%= @user.email %></h1>
    <h3>Member since: <%= @user.created_at.strftime("%B %d, %Y") %></h3>
    <h4><%= @user.comments.count %> total comments</h4>
    <h4><%= @user.comments.where(rating: '5_stars').count %> total 5 star reviews</h4>
  </div>
  <div class = "clear"></div>
</div>

<br /><br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
<h1 class = "text-center">My Comments</h1>
<br /><br />
  <% @user.comments.each do |comment| %>
    <div class = "col-xs-12 col-sm-6">
    <h2 class = "text-center"><%= link_to comment.place.name if comment.place, place_path(comment.place) %></h2>
      <blockquote>
        <% if comment.humanized_rating.present? %>
          <%= comment.message %><br />
          <b><%= comment.humanized_rating %></b>
        <% end %>
        <small><%= comment.user.email %></small>
      </blockquote>
    </div>
  <% end %>
</div>

<div class = "clear"></div>
<br /><br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
<h1 class = "text-center">My Places</h1>
<br /><br />
  <% @user.places.each do |place| %>
    <div class = "col-xs-12 col-sm-6">
      <h1><%= link_to place.name if comment.place, place_path(place) %></h1>
      <i><%= place.address %></i><br />
      <p><%= place.description %></p><br />

      <% if place.user.present? %>
        <small><%= place.user.email %></small>
      <% end %>

      <% if place.comments.present? %>
        <div class="col-xs-12">
          <blockquote>
            <% if place.comments.last.humanized_rating.present? %>
              <b><%= place.comments.last.humanized_rating %></b> -
            <% end %>
            <%= place.comments.last.message %>
            <small><%= place.comments.last.user.email %></small>
          </blockquote>
        </div>
      <% end %>
    </div>
<% end %>
</div>